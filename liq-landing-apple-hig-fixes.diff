--- a/panda.config.ts
+++ b/panda.config.ts
@@ -98,6 +98,7 @@
       radii: {
         xl: { value: "0.75rem" },
         "2xl": { value: "1rem" },
+        full: { value: "9999px" },
         glass: { value: "1rem" },
       },
       shadows: {
--- a/src/pandaStyles.ts
+++ b/src/pandaStyles.ts
@@ -6,6 +6,14 @@
     fontFamily: "sans",
     fontWeight: 600,
     borderRadius: "full", /* capsule */
+    display: "inline-flex",
+    alignItems: "center",
+    justifyContent: "center",
+    gap: "0.375rem",
+    lineHeight: 1.2,
+    cursor: "pointer",
+    WebkitTapHighlightColor: "transparent",
+    _focusVisible: { outline: "none", boxShadow: "0 0 0 3px color-mix(in oklab, var(--colors-apple-blue), transparent 50%)" },
     transitionProperty: "all",
     transitionDuration: "150ms",
     transform: "translateZ(0)",
--- a/src/components/CodeBlock.tsx
+++ b/src/components/CodeBlock.tsx
@@ -32,7 +32,7 @@
   padding: "0.5rem",
   backgroundColor: "rgba(255, 255, 255, 0.1)",
   border: "1px solid rgba(255, 255, 255, 0.2)",
-  borderRadius: "0.5rem",
+  borderRadius: "9999px",
   color: "white",
   cursor: "pointer",
   fontSize: "0.875rem",
--- a/styled-system/styles.css
+++ b/styled-system/styles.css
@@ -277,6 +277,7 @@
     --radii-xl: 0.75rem;
     --radii-2xl: 1rem;
     --radii-glass: 1rem;
+    --radii-full: 9999px;
     --shadows-sm: 0 1px 2px rgba(0,0,0,0.06);
     --shadows-md: 0 10px 20px rgba(0,0,0,0.08);
     --shadows-lg: 0 20px 30px rgba(0,0,0,0.12);
@@ -395,7 +396,7 @@
 }
 
   .bdr_full {
-    border-radius: full;
+    border-radius: var(--radii-full);
 }
 
   .bd-w_1px {
